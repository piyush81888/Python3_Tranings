"""
List of data types

Data Type: By default some of the options are present to store the data
"""

"""
IMMUTABLE TYPES
    (After creating variable with some values, 
    that value CAN'T be changed throught the program)

    - Numbers:
        -- We have option to store numbers like int, float etc
    - String:
        -- We have option to store text data like name, email-id etc
        -- Automatically index number will be assigned to each value
    - Tuple:
        -- We have option to store multiple values like list of names
        -- We can keep DUPLICATE values
        -- Automatically index number will be assigned to each value
    - Frozenset:
        -- We have option to store multiple values like list of names
        -- We can keep UNIQUE values
        -- NO index to each value

MUTABLE TYPES
(After creating variable with some values, 
that value CAN be changed throught the program)

    - List:
        -- We have option to store multiple values like list of names
        -- We can keep DUPLICATE values
        -- Automatically index number will be assigned to each value
    - Set:
        -- We have option to store multiple values like list of names
        -- We can keep UNIQUE values
        -- NO index to each value
    - Dictionary:
        -- We have option to store multiple values like list of names
        -- We can keep DUPLICATE values
        -- We-can/we-have to provide index to each values
"""